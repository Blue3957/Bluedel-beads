{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}

{% block body %}
<div class="container">
	<div class="card">
		<div class="card-header">
			<h3>Links</h3>
		</div>
		<div class="card-body">
			<p class="card-text">
				<a href="{{ path("perler_palette_default") }}">Perler Palette display</a><br>
				<a href="{{ path("perler_pattern") }}">Perler Pattern maker</a>
			</p>
		</div>
	</div>
</div>
{% endblock %}
